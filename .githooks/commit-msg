#!/usr/bin/env bash
# Strip Cursor co-author trailers if injected after the editor/hook chain.
set -euo pipefail
msg_file="${1:?}"
tmp="$(mktemp)"
sed '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "$msg_file" >"$tmp"
mv "$tmp" "$msg_file"
